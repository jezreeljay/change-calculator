// Write your JavaScript here

function calculateChange(due, given) {
    due = document.getElementById('ammount-due').value;
    given = document.getElementById('ammount-given').value;

    let cents, quarters, dimes, nickels, pennies;


}

getElementById('calculate-change').addEventListener('click',calculateChange(due, given));